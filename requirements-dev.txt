# 开发与测试依赖（运行依赖见 requirements.txt）
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pytest-timeout>=2.3
ruff>=0.5
mypy>=1.10
bandit>=1.7
# types-requests 必须装：mypy 否则报 [import-untyped]（不被 --ignore-missing-imports 覆盖）
types-requests>=2.31
